{% extends 'base.html.twig' %}

{% block title %}products{% endblock %}

{% block body %}
    <div class="container">
            <h1></h1>

            <div class="row custom-line">
                {% for product in products %}
                    <div class="col-sm-10 col-md-6 col-lg-4 mt-4 mb-4 ">
                        <div class="card h-100 px-3">
                            <div class="card-header bg-transparent border-dark">
                                {{ product.name }}
                            </div>
                            <div class="card-body text-primary">
                                <img src="{{ vich_uploader_asset(product) }}" class="card-img-top" alt="{{ product.name }}">
                        </div>
                            <div>
                                <a href="{{ path('app_cart_add', {'id': product.id}) }}" class="btn btn-success float-right">
{#                                    <i class="fas fa-shopping-cart mr-1"></i>#}
                                    Ajouter à mon panier
                                </a>
                                <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-success float-right">
{#                                    <i class="fas fa-shopping-cart mr-1"></i>#}
                                    Description
                                </a>
                            </div>
                        <div class=" card-footer bg-transparent border-dark">
{#                                <br><a href="{{ path('app_product', {id: product.id }) }}"#}
{#                                       class="stretched-link"></a>#}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
{% endblock %}
